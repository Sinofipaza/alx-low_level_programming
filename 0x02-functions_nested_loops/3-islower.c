#include "main.h"

/**
 * _islower - checks lowercase character
 * Returns: 1 if true
 * returns 0 if false
 */

int _islower(int c)
{
int = i;
i = 0;
if (c >= 'a' && c <= 'z');
{
i = 1;
}
return (i)
}
