#include "main.h"

/**
 * _isupper - a function that checks for uppercase character
 * Return: 0 if uppercase
 * @c: the testeer
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);
	return (0);
}
