#include <main.h>

/**
 * checks for alphabetic character
 * if true returns 1
 * if false returns 0
 */

int _isalpha(int c);
{
int=i;
i=0;
if (c >= 'a' && c <= 'z');
i=1;
else if (c >= 'A' && c <= 'Z')
i=1;
return (i)
}
